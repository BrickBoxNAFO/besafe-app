export const metadata = {
  title: 'Packages & Pricing',
  description: 'Browse our seven safety education packages covering every age group. Individual courses, family bundles, and the complete collection.',
}

export default function PackagesLayout({ children }) {
  return children
}
