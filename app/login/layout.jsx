export const metadata = {
  title: 'Log In',
  description: 'Sign in to your HomeSafeEducation account to access your courses and learning materials.',
  alternates: { canonical: '/login' },
}

export default function LoginLayout({ children }) {
  return children
}
