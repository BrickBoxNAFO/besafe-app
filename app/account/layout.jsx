export const metadata = {
  title: 'My Account',
  description: 'Manage your HomeSafeEducation account settings, subscription, and profile.',
}

export default function AccountLayout({ children }) {
  return children
}
