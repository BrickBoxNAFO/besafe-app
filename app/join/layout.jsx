export const metadata = {
  title: 'Join Family',
  description: 'Accept a family invitation and join a HomeSafeEducation family group.',
}

export default function JoinLayout({ children }) {
  return children
}
